<% @page_title = "Update Sub Category" %>
<% @page_desc = "This area is to update a Sub Category" %>

<%= link_to '<< Back to Sub Categories', sections_path(:category_id => @category.id), class: "btn btn-link" %>

<%= form_for(@section, :url => section_path(@section, :category_id => @category.id), html: {class: 'form-horizontal'}) do |f| %>
    <%= render(:partial => 'form', :locals => {:f => f}) %> 
<% end %>